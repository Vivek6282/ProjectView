[project]
name = "project-view"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "django>=5.0",
    "reportlab>=4.4.10",
    "gunicorn",
    "whitenoise[broccoli]",
    "dj-database-url",
    "PyMySQL",
    "python-dotenv",
]
